-# .content_post{:style => "background-image:url(#{post.image});"}
-#   .more
-#     %span= image_tag 'arrow_top.png'
-#     %ul.more_list
-#       %li
-#         = link_to '編集', "/posts/#{post.id}/edit", method: :get
-#       %li
-#         = link_to '削除', "/posts/#{post.id}", method: :delete
-#   - simple_format(post.text)
-#   -# .post__image
-#   -#   =post.image
-#   -# .post__movie
-#   -#   =post.movie
-#   .post__text
-#     = post.text 
-#   .post__date
-#     =post.created_at


.post
  .post_user_nickname 
    = link_to "/users/#{post.user.id}", class: 'post_user_name_link' do
      = post.user.nickname
      さんによるShare★
  .more
    %span= image_tag 'arrow_top.png'
    %ul.more_list
      %li
        = link_to '編集', "/posts/#{post.id}/edit", method: :get
      %li
        = link_to '削除', "/posts/#{post.id}", method: :delete 

  - simple_format(post.text)

  .post__text
    = post.text 
  .post__image
    = link_to "/posts/#{post.id}" do
      = image_tag "#{post.image}", :width => '500', :height => '400'
  .post__movie
    =post.movie  
  .post__date
    = post.created_at.to_s(:datetime_jp)
  
